package day01;

import java.util.Scanner;

// 자바의 소스코드는 클래스가 기본 구성단위이다.
// 자바에서 클래스를 제외한 나머지 요소는 모두 첫글자를 소문자로 작성한다.
// (변수, 함수, 접근제한자, 패키지, 프로젝트
// 단, 클래스의 static final 멤버는 모든 글자를 대문자로 작성하며_로 단어를 구분한다.
// 변수 및 함수의 이름은 소문자, _로 시작할 수 있다. (숫자는 첫번째 글자가 될 수 없다.)



/*
 *  dao.selectAll()   : 객체의 매서드를 호출
 *  Dao.getInstance() : 클래스의 정적 매서드를 호출
 * 
 */


public class Ex01 {
	public static void main(String[] args) {
		
		// 자료형 (data type)
		/*
		 * 1. primitive type (단순 자료형, 원시 자료형) 기본타입
		 * 		소문자로 시작하며 총 8개의 기초적인 자료형이 있다.
		 *		복잡한 데이터가 아니므로, 변수에 데이터를 직접 저장한다.
		 *		순수하게 데이터를 저장할 용도로 쓰인다.
		 *		단순한 자료 하나를 저장하는 것에 쓰인다.
		 *
		 *	☆	boolean	1바이트	논리값		true, false								대표자료형
		 *		byte	1바이트	정수(8비트)	-128 ~ +127			갯수:2[8]
		 *		short	2바이트	정수(16비트)	-32768 ~ +32767		갯수:2[16]
		 *		char	2바이트	정수(글자)		0 ~ 65535			갯수:2[16]
		 *	☆	int		4바이트	정수(32비트)	약-21억 ~ 약+21억		갯수:2[32] 42억9천만	대표자료형
		 *		long	8바이트	정수(64비트)	-922조~ +922조		갯수:2[64]
		 *		float	4바이트	실수		부동소수점
		 *	☆	double	8바이트	실수													대표자료형
		 *
		 *
		 *
		 * 2. Reference Type(참조 자료형)
		 * 
		 * 		참조변수의 크기는 일정하다.(4바이트 or 8바이트)
		 * 		단순한 자료와 복잡한 자료를 다루는 방식이 다르다 즉, 복잡합 자료를 다루는 방식이 참조 자료형이다.
		 * 
		 * - Array Type : 배열
		 * - class Type : 클래스, 인터페이스 등 ....
		 * 
		 * 
		 */
		
		
		// 단일 데이터를 메모리어 저장하고 싶다 -> 크기에 맞는 변수
		// 같은 자료형의 데이터를 여러개 묶어서 저장하고 싶다. -> 배열
		
		// 서로 다른 자료형의 데이터를 묶어서 저장하려면 새로은 자료형을 정의해야 한다.
		// 같은 자료형간만 상호작용이 가능하다.
		
		// 구조체 in c언어			변수만 묶어서 자료형을 만든다
		// 클래스 in Java(c++)		변수와 함수를 묶어서 자료형을 만든다.
		
		Scanner sc = new Scanner(System.in);
		System.out.print("itbank라고 입력하세요 : ");
		String str1 = sc.nextLine();
		
		String str2 = "itbank";
		String str3 = new String("itbank");
		String str4 = "itbank";
		
		System.out.println(str1 == str2);
		System.out.println(str1 == str3);
		System.out.println(str2 == str3);
		System.out.println(str2 == str4);
		
		sc.close();
		
		
		// 별도의 메모리에 저장되어 있는것을 참조하는 것이므로 4번의 경우 두개의 값이 같다고 나오는 것은 메인 함수에서는 따로 저장되어있지만
		// 참조하는 곳의 메모리는 
		// Scanner는 사용 후 무조건 닫아줘야 한다.
		
		
	}
	

}



// 메인함수가 첫번째 실행되는 함수다.
// 가수는 1.0에서 2.0 사이의 수를 의미합니다.
// 2의 제곱은 외워두면 좋음.2,4,8,16,32,64,128,256,512,1024,2048,4096,8192,16384,32768,65536
// 42억9천만(32비트) 전세계의 인구보다 부족하다.
// 그래서 ipv6(128비트)가 등장한다.
// int가 대표자료형인 이유는 옛날 개발자들이 int라는 자료형을 많이 썼기 때문에 대표자료형으로 굳어짐(int의 특수성)
// 



